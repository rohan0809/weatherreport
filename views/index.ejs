<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Weather App</title>
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(to right, #fbc2eb, #a6c0fe);
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            color: #333;
        }
        .container {
            background: #fff;
            border-radius: 12px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            padding: 30px;
            width: 100%;
            max-width: 600px;
            text-align: center;
        }
        h1 {
            color: #ff6f61;
            margin-bottom: 20px;
            font-size: 2em;
        }
        form {
            margin-bottom: 20px;
        }
        label {
            font-size: 1.2em;
            color: #333;
        }
        input[type="text"] {
            padding: 10px;
            font-size: 1em;
            border: 2px solid #ff6f61;
            border-radius: 4px;
            margin-right: 10px;
            width: 60%;
            max-width: 300px;
            outline: none;
            transition: border-color 0.3s;
        }
        input[type="text"]:focus {
            border-color: #ff8a80;
        }
        button {
            padding: 10px 20px;
            font-size: 1em;
            color: #fff;
            background: #ff6f61;
            border: none;
            border-radius: 4px;
            cursor: pointer;
            transition: background-color 0.3s, transform 0.3s;
        }
        button:hover {
            background: #ff8a80;
            transform: scale(1.05);
        }
        .weather-info {
            background: #fff3e0;
            border: 2px solid #ffcc80;
            padding: 20px;
            border-radius: 8px;
            margin-top: 20px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
        .weather-info h2 {
            color: #ff6f61;
            margin: 0;
            font-size: 1.5em;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Weather App</h1>
        <form action="/" method="post">
            <label for="cityname">Enter City Name:</label>
            <input type="text" id="cityname" name="cityname" required>
            <button type="submit">Get Weather</button>
        </form>
        <% if(locals.temparature && locals.deep) { %>
            <div class="weather-info">
                <h2>Temperature: <%= temparature.temp %>°C</h2>    
                <h2>Description: <%= deep.descrip %></h2>
            </div>
        <% } %>
    </div>
</body>
</html>
